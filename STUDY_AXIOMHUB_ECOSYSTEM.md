# 🏙️ دراسة المدينة المحورية - AxiomHub Ecosystem
## مشروع النظام البيئي المتكامل لـ AxiomHub

**تاريخ الدراسة:** 6 نوفمبر 2025  
**الحالة:** قيد التخطيط والتنفيذ  
**الرؤية:** بناء منصة رقمية محورية متكاملة تدير عدة أنظمة فرعية مستقلة

---

## 📋 فهرس المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة)
2. [الهيكلية المعمارية](#الهيكلية-المعمارية)
3. [الأنظمة الفرعية](#الأنظمة-الفرعية)
4. [البنية التقنية](#البنية-التقنية)
5. [قاعدة البيانات](#قاعدة-البيانات)
6. [نظام المصادقة الموحد](#نظام-المصادقة)
7. [خطة التنفيذ](#خطة-التنفيذ)
8. [السوق الكبير (Marketplace)](#السوق-الكبير)

---

## 🎯 نظرة عامة على المشروع {#نظرة-عامة}

### المفهوم الأساسي

**AxiomHub** هي منصة رقمية محورية (Hub Platform) تعمل كنظام أم (Parent System) تدير وتوحد عدة أنظمة فرعية مستقلة (Child Systems). كل نظام فرعي:

- ✅ له **نطاق فرعي خاص** (Subdomain)
- ✅ له **مجلد مستقل** (Separate Folder)
- ✅ له **قاعدة بيانات منفصلة** (Separate Database)
- ✅ يتصل بـ **نظام المصادقة الموحد** (SSO)
- ✅ يشارك في **الموارد المشتركة** (Shared Resources)

### الفوائد الاستراتيجية

| الفائدة | الوصف |
|---------|-------|
| **قابلية التوسع** | إضافة أنظمة جديدة بدون تأثير على الأنظمة الموجودة |
| **الصيانة السهلة** | تحديث نظام واحد دون إيقاف بقية الأنظمة |
| **الأمان المعزز** | عزل البيانات بين الأنظمة المختلفة |
| **الأداء الأفضل** | توزيع الحمل على خوادم متعددة |
| **المرونة** | كل نظام يمكن أن يعمل بتقنيات مختلفة إذا لزم الأمر |

---

## 🏗️ الهيكلية المعمارية {#الهيكلية-المعمارية}

### نموذج Microservices Architecture

```
AxiomHub (المنصة الأم)
    │
    ├─── Core Services (الخدمات الأساسية)
    │    ├── Authentication (المصادقة)
    │    ├── User Management (إدارة المستخدمين)
    │    ├── Company Management (إدارة الشركات)
    │    ├── Billing & Subscriptions (الفوترة والاشتراكات)
    │    └── API Gateway (بوابة API الموحدة)
    │
    ├─── Child Systems (الأنظمة الفرعية)
    │    ├── Axiom Inspect (نظام التفتيش)
    │    ├── Axiom Marketplace (السوق الكبير)
    │    ├── Axiom Orders (نظام الطلبات)
    │    └── [Future Systems...]
    │
    └─── Shared Resources (الموارد المشتركة)
         ├── Models (النماذج المشتركة)
         ├── Middleware (الوسائط المشتركة)
         ├── Utilities (الأدوات المساعدة)
         └── Configurations (الإعدادات)
```

### مخطط النطاقات (Domains Structure)

```
axiomhub.com (المنصة الأم)
├── www.axiomhub.com (البوابة الرئيسية)
├── api.axiomhub.com (API المركزي)
├── inspect.axiomhub.com (نظام التفتيش)
├── marketplace.axiomhub.com (السوق الكبير)
├── orders.axiomhub.com (نظام الطلبات)
├── hr.axiomhub.com (الموارد البشرية - مستقبلي)
├── finance.axiomhub.com (المالية - مستقبلي)
└── inventory.axiomhub.com (المخزون - مستقبلي)
```

---

## 🌐 الأنظمة الفرعية {#الأنظمة-الفرعية}

### 1. Axiom Inspect (نظام التفتيش)

**النطاق:** `inspect.axiomhub.com`  
**المجلد:** `/axiom-inspect`  
**المنفذ:** `3001`  
**قاعدة البيانات:** `axiomhub_inspect`

**الوظائف:**
- ✅ إدارة مشاريع البناء والتشييد
- ✅ إدارة المخططات الهندسية (PDF/Images)
- ✅ إضافة نقاط التفتيش على المخططات
- ✅ تتبع جودة التنفيذ ونسب الإنجاز
- ✅ إنشاء التقارير والإحصائيات

**المستخدمون:**
- مهندسون مدنيون
- مقاولون
- مشرفو المشاريع
- مديرو الشركات

**الحالة الحالية:**
- ✅ موجود ويعمل بكامل الميزات
- ✅ واجهة ثنائية اللغة (عربي/إنجليزي)
- ✅ نظام مصادقة JWT
- ⏳ يحتاج ربط بنظام SSO

---

### 2. Axiom Marketplace (السوق الكبير)

**النطاق:** `marketplace.axiomhub.com`  
**المجلد:** `/axiom-marketplace`  
**المنفذ:** `3002`  
**قاعدة البيانات:** `axiomhub_marketplace`

**الوظائف:**
- 🛒 سوق إلكتروني متعدد البائعين (Multi-vendor)
- 🏪 تسجيل المتاجر (البائعين)
- 📦 عرض المنتجات في أقسام منظمة
- 💳 نظام الدفع والطلبات
- 🚚 إدارة الشحن والتوصيل
- 💰 نظام العمولات

**الأقسام المقترحة للـ MVP:**

#### القسم الأول: مواد البناء 🏗️
```
مواد البناء (الرئيسي)
├── الأرضيات والجدران
│   ├── بلاط السيراميك
│   ├── باركيه خشبي
│   ├── رخام وجرانيت
│   └── ورق جدران
├── الأخشاب ومواد التشطيب
│   ├── أخشاب طبيعية
│   ├── خشب MDF
│   ├── جبس بورد
│   └── ألواح ديكورية
├── الدهانات والعوازل
│   ├── دهانات داخلية
│   ├── دهانات خارجية
│   ├── عوازل مائية
│   └── عوازل حرارية
└── الأدوات والمعدات
    ├── أدوات يدوية
    ├── معدات كهربائية
    ├── سقالات وملحقات
    └── معدات السلامة
```

#### القسم الثاني: الأثاث والديكور 🪑
```
الأثاث والديكور (الرئيسي)
├── أثاث غرف النوم
│   ├── أسرّة
│   ├── دواليب
│   ├── كومودينو
│   └── تسريحات
├── أثاث المجالس والصالات
│   ├── كنب
│   ├── طاولات قهوة
│   ├── مكتبات
│   └── وحدات تلفزيون
├── الإضاءة والتحف
│   ├── ثريات
│   ├── إضاءة مخفية
│   ├── لوحات فنية
│   └── تحف ديكورية
└── السجاد والمفروشات
    ├── سجاد تركي
    ├── سجاد إيراني
    ├── ستائر
    └── وسائد ديكورية
```

**سبب اختيار هذين القسمين:**
1. ✅ **التكامل الطبيعي:** البناء → التشطيب → الأثاث (رحلة العميل)
2. ✅ **السوق الكبير:** طلب مستمر على مدار السنة
3. ✅ **الخبرة الموجودة:** الشركة لديها خبرة في مجال البناء (من Inspect)
4. ✅ **البائعون الجاهزون:** محلات مواد البناء ومعارض الأثاث منتشرة
5. ✅ **هامش الربح المناسب:** منتجات متوسطة/عالية القيمة

**نماذج العمل:**

| النموذج | الوصف | نسبة العمولة المقترحة |
|---------|-------|------------------------|
| **نموذج المتجر** | البائع يسجل متجراً ويعرض منتجاته | 5-10% من كل عملية بيع |
| **نموذج المندوب** | مندوب مبيعات يعرض منتجات بائعين | 10-15% عمولة المندوب + 5% للمنصة |
| **نموذج الاشتراك** | البائع يدفع رسوم شهرية ثابتة | 99-299 ريال/شهر |

**الحالة الحالية (محدثة):**
- ✅ تم بناء خدمة السوق كمicroservice على منفذ مستقل (3002) في `market-server.js`
- ✅ واجهة عرض المنتجات + البحث + الترقيم + الفلاتر (`marketplace-index.html` + سكربتات معيارية)
- ✅ طبقة كشف قاعدة الـ API تلقائياً + شارة حالة (Badge) + تلميحات تشخيصية
- ✅ دعم CRUD للبائع (صفحات: `seller-dashboard.html`, `seller-products.html` + سكربتات `sellerProducts.js`, `sellerAPI.js`)
- ✅ دعم رفع الوسائط (multer + sharp) ومتغيرات المنتج (Variants)
- ✅ دعم السلة والطلبات الأساسية (مسارات: `/api/market/cart`, `/api/market/orders`)
- ✅ طبقة ترتيب ديناميكي (Rank Auto-Tune) + تتبع التفاعلات (مشاهدة/نقر)
- ✅ توسعة الحقول عبر `expand` (vendor, warehouse, media, variants, all) + دعم ETag/304 + Cache داخلي
- ✅ مسارات إدارية (أوزان الترتيب، إعادة الفهرسة، المقاييس، التدقيق) مع حراسة أدوار
- 🔐 مصادقة JWT + حقن تلقائي للرأس Authorization
- 📊 تغطية اختبارات (بحث، ترقيم، حالات فشل الشبكة، الترتيب، التوسعة، ETag) جميعها ناجحة
- 🔄 مرحلة التحضير للربط مع SSO والنشر كنطاق مستقل

### 🔍 التوثيق التفصيلي للسوق

#### 1. دورة حياة المنتج
1. إنشاء: `POST /api/market/seller/products` (يحفظ SKU + الأساسيات)
2. رفع صور: `POST /api/market/seller/products/:id/images`
3. إضافة متغيرات: `POST /api/market/seller/products/:id/variants`
4. تعديل: `PUT /api/market/seller/products/:id`
5. تعطيل/حذف: `DELETE /api/market/seller/products/:id`
6. أي تعديل حساس → `cache.bumpVersion(country)` لإعادة بناء نتائج البحث

#### 2. واجهة البائع
- تعتمد على تخزين التوكن في LocalStorage
- فلاتر وفرز + تمرير صفحي + Scroll مراقب
- تمييز حالة المخزون والأصناف غير المكتملة (صور مفقودة / متغيرات ناقصة)

#### 3. البحث والعرض
- `GET /api/market/products?page=&limit=&q=&sort=&expand=&mode=&mediaFields=`
- حساب إجمالي الصفحات + دعم `total`, `page`, `limit` في الاستجابة
- تكامل ETag للصفحة الأولى + 304 لتقليل الحمل

#### 4. تجربة العميل
1. كشف الـ API Base (محاولات متعددة + تسجيل مدة الكشف)
2. تحميل الصفحة الأولى (Rank Sort)
3. تفاعل (نقر/عرض) → يسجل لتحسين ترتيب لاحق
4. إضافة للسلة → حفظ حالة للطلب النهائي

#### 5. السلة والطلب
- `POST /api/market/cart` إضافة عنصر
- `GET /api/market/cart` عرض محتوى
- `POST /api/market/orders` تحويل السلة إلى طلب
- `GET /api/market/orders/:id` تفاصيل
- Webhook دفع مستقبلي (Stub جاهز)

#### 6. التحكم الإداري
- `GET /api/market/admin/metrics` مؤشرات تشغيل
- `POST /api/market/admin/reindex` إعادة فهرسة
- `POST /api/market/admin/rank/weights` ضبط يدوي
- `GET /api/market/rank/audit` تدقيق تاريخ الأوزان

#### 7. المراقبة (Observability)
- عدادات يومية (منتجات، تفاعلات، طلبات)
- تسجيل زمن تنفيذ البحث + حالة الاستجابة + حجم البيانات

#### 8. الأدوار
| الدور | الصلاحيات |
|-------|-----------|
| super_admin | أوزان، إعادة فهرسة، مقاييس |
| seller_owner/staff | إدارة المنتجات والوسائط |
| user | سلة، طلبات، رسائل |
| anonymous | بحث وعرض عام |

#### 9. الأمن
- JWT + التحقق من الدور قبل المسارات الحساسة
- مراجعة لاحقة مقترحة: معدل طلبات (Rate Limit) + تفتيش وسائط أمنياً

#### 10. التوسع القادم
- ربط دفع حقيقي + مراجعات/تقييمات + Wishlist + مقارنة منتجات
- نشر مستقل خلف API Gateway + فصل خدمة الوسائط إلى CDN

> السوق أصبح جاهزاً للانتقال إلى إطلاق تجريبي (Beta) بعد دمج SSO والدفع.

---

### 3. Axiom Orders (نظام الطلبات والمشتريات)

**النطاق:** `orders.axiomhub.com`  
**المجلد:** `/axiom-orders`  
**المنفذ:** `3003`  
**قاعدة البيانات:** `axiomhub_orders`

**الوظائف:**
- 📋 إدارة طلبات الشراء للشركات (B2B)
- ✅ سير عمل الموافقات (Approval Workflow)
- 📊 تتبع الميزانيات والمشتريات
- 📦 إدارة الموردين
- 📈 تقارير المشتريات والتكاليف

**الفرق عن Marketplace:**
- Marketplace: للمستهلكين (B2C) والشراء الفوري
- Orders: للشركات (B2B) مع موافقات متعددة وطلبات كبيرة

**الحالة الحالية:**
- ⏳ لم يبدأ التطوير بعد
- 📋 مخطط له في المراحل المتقدمة

---

## 💻 البنية التقنية {#البنية-التقنية}

### هيكل المجلدات الكامل

```
D:\HDD\test1\Company_App\
│
├── axiomhub-core\                    # المنصة الأم (الخادم المركزي)
│   ├── server.js                     # الخادم الرئيسي (Port 3000)
│   ├── package.json
│   ├── .env                          # المتغيرات البيئية المشتركة
│   │
│   ├── shared\                       # الموارد المشتركة بين جميع الأنظمة
│   │   ├── models\
│   │   │   ├── User.js              # نموذج المستخدم الموحد
│   │   │   ├── Company.js           # نموذج الشركة
│   │   │   └── Subscription.js      # نموذج الاشتراكات
│   │   │
│   │   ├── middleware\
│   │   │   ├── auth.js              # المصادقة الموحدة (JWT)
│   │   │   ├── rateLimiter.js       # تحديد معدل الطلبات
│   │   │   ├── cors.js              # إعدادات CORS
│   │   │   └── errorHandler.js      # معالجة الأخطاء المركزية
│   │   │
│   │   └── utils\
│   │       ├── jwt.js               # أدوات JWT
│   │       ├── encryption.js        # التشفير
│   │       ├── logger.js            # تسجيل الأحداث
│   │       └── validator.js         # التحقق من البيانات
│   │
│   ├── core-api\                     # API المركزي للمنصة الأم
│   │   ├── routes\
│   │   │   ├── auth.js              # مسارات تسجيل الدخول الموحد
│   │   │   ├── companies.js         # إدارة الشركات
│   │   │   ├── users.js             # إدارة المستخدمين
│   │   │   └── subscriptions.js     # إدارة الاشتراكات
│   │   │
│   │   └── controllers\
│   │       ├── authController.js
│   │       ├── companyController.js
│   │       └── userController.js
│   │
│   ├── gateway\                      # API Gateway (توجيه الطلبات)
│   │   ├── proxy.js                  # توجيه الطلبات للأنظمة الفرعية
│   │   └── loadBalancer.js           # توزيع الحمل
│   │
│   └── public\                       # الواجهة الأمامية للبوابة الرئيسية
│       ├── index.html                # الصفحة الرئيسية
│       ├── login.html                # صفحة تسجيل الدخول الموحدة
│       ├── register.html             # صفحة التسجيل
│       └── dashboard.html            # لوحة التحكم الرئيسية
│
├── axiom-inspect\                    # نظام التفتيش (موجود حالياً)
│   ├── server.js                     # Port 3001
│   ├── package.json
│   ├── .env
│   ├── Coreflowhub\                  # الواجهة الأمامية
│   │   ├── index.html
│   │   ├── inspect_dashboard_new.html
│   │   ├── js\
│   │   └── css_pirobox\
│   ├── models\
│   │   ├── InspectModels.js
│   │   └── server_user.js
│   ├── routes\
│   │   ├── server_inspect.js
│   │   └── server_auth.js
│   └── controllers\
│       └── server_inspect_controller.js
│
├── axiom-marketplace\                # السوق الكبير (جديد)
│   ├── server.js                     # Port 3002
│   ├── package.json
│   ├── .env
│   │
│   ├── public\                       # الواجهة الأمامية
│   │   ├── index.html               # الصفحة الرئيسية (عرض الأقسام)
│   │   ├── category.html            # صفحة القسم (عرض المنتجات)
│   │   ├── product.html             # صفحة تفاصيل المنتج
│   │   ├── cart.html                # سلة التسوق
│   │   ├── checkout.html            # صفحة الدفع
│   │   ├── seller-dashboard.html   # لوحة تحكم البائع
│   │   ├── seller-products.html    # إدارة المنتجات
│   │   ├── seller-orders.html      # إدارة الطلبات
│   │   ├── seller-finance.html     # المالية والأرباح
│   │   ├── js\
│   │   └── css\
│   │
│   ├── models\
│   │   ├── Store.js                 # نموذج المتجر
│   │   ├── Category.js              # نموذج الأقسام
│   │   ├── Product.js               # نموذج المنتج
│   │   ├── Order.js                 # نموذج الطلب
│   │   ├── OrderItem.js             # نموذج عنصر الطلب
│   │   ├── Cart.js                  # نموذج السلة
│   │   └── Review.js                # نموذج التقييم
│   │
│   ├── routes\
│   │   ├── stores.js
│   │   ├── products.js
│   │   ├── orders.js
│   │   └── categories.js
│   │
│   └── controllers\
│       ├── storeController.js
│       ├── productController.js
│       └── orderController.js
│
└── axiom-orders\                     # نظام الطلبات (مستقبلي)
    ├── server.js                     # Port 3003
    ├── package.json
    └── ... (سيتم تطويره لاحقاً)
```

### التقنيات المستخدمة

| المكون | التقنية | الإصدار |
|--------|---------|---------|
| **Backend** | Node.js + Express | v18.x + v4.x |
| **Database** | MongoDB | v6.x |
| **Authentication** | JWT (JSON Web Tokens) | - |
| **Frontend** | Vanilla JavaScript | ES6+ |
| **Styling** | Custom CSS | - |
| **Process Manager** | PM2 | v5.x |
| **Reverse Proxy** | Nginx | v1.24+ |
| **Version Control** | Git | - |

---

## 🗄️ قاعدة البيانات {#قاعدة-البيانات}

### استراتيجية التقسيم

كل نظام فرعي له **قاعدة بيانات منفصلة** لعزل البيانات وتحسين الأداء:

```javascript
// axiomhub-core
mongoose.connect('mongodb://localhost:27017/axiomhub_core');

// axiom-inspect
mongoose.connect('mongodb://localhost:27017/axiomhub_inspect');

// axiom-marketplace
mongoose.connect('mongodb://localhost:27017/axiomhub_marketplace');

// axiom-orders
mongoose.connect('mongodb://localhost:27017/axiomhub_orders');
```

### المجموعات المشتركة (في axiomhub_core)

#### 1. Users (المستخدمون)

```javascript
{
  _id: ObjectId,
  email: String,              // البريد الإلكتروني (فريد)
  username: String,           // اسم المستخدم (فريد)
  password: String,           // كلمة المرور المشفرة (bcrypt)
  name: String,               // الاسم الكامل
  phone: String,              // رقم الهاتف
  profileImage: String,       // رابط صورة الملف الشخصي
  
  role: {
    type: String,
    enum: ['Admin', 'CompanyOwner', 'Employee', 'Buyer', 'Seller', 'Driver'],
    default: 'Buyer'
  },
  
  companyId: ObjectId,        // معرف الشركة (إذا كان موظفاً)
  
  permissions: {
    inspect: [String],        // ['read', 'write', 'admin']
    marketplace: [String],    // ['buyer', 'seller', 'admin']
    orders: [String]          // ['buyer', 'approver', 'admin']
  },
  
  addresses: [{               // عناوين الشحن (للمشترين)
    type: String,             // 'home', 'work', 'other'
    street: String,
    city: String,
    region: String,
    postalCode: String,
    isDefault: Boolean
  }],
  
  isActive: Boolean,
  isEmailVerified: Boolean,
  lastLogin: Date,
  createdAt: Date,
  updatedAt: Date
}
```

#### 2. Companies (الشركات)

```javascript
{
  _id: ObjectId,
  name: String,                    // اسم الشركة
  nameEn: String,                  // الاسم بالإنجليزية
  logo: String,                    // رابط اللوجو
  description: String,             // وصف الشركة
  
  legalInfo: {
    commercialRegister: String,    // السجل التجاري
    taxNumber: String,             // الرقم الضريبي
    licenseNumber: String,         // رقم الرخصة
    expiryDate: Date              // تاريخ انتهاء الرخصة
  },
  
  contact: {
    email: String,
    phone: String,
    address: String,
    website: String
  },
  
  ownerId: ObjectId,               // معرف صاحب الشركة (User)
  
  subscription: {
    plan: String,                  // 'free', 'basic', 'pro', 'enterprise'
    startDate: Date,
    endDate: Date,
    features: [String],            // ['inspect', 'marketplace', 'orders']
    status: String                 // 'active', 'expired', 'cancelled'
  },
  
  settings: {
    language: String,              // 'ar', 'en'
    timezone: String,
    currency: String               // 'SAR', 'USD'
  },
  
  isActive: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

### المجموعات في Marketplace (axiomhub_marketplace)

#### 1. Categories (الأقسام)

```javascript
{
  _id: ObjectId,
  name: {
    ar: String,                    // اسم القسم بالعربية
    en: String                     // اسم القسم بالإنجليزية
  },
  slug: String,                    // للرابط: /category/building-materials
  description: {
    ar: String,
    en: String
  },
  image: String,                   // صورة القسم
  icon: String,                    // أيقونة القسم
  
  parentId: ObjectId,              // null للأقسام الرئيسية
  level: Number,                   // 0 للرئيسي, 1 للفرعي, إلخ
  order: Number,                   // ترتيب العرض
  
  isActive: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

#### 2. Stores (المتاجر)

```javascript
{
  _id: ObjectId,
  ownerUserId: ObjectId,           // معرف صاحب المتجر (User)
  
  name: {
    ar: String,
    en: String
  },
  slug: String,                    // للرابط: /store/golden-wood-store
  description: {
    ar: String,
    en: String
  },
  logo: String,
  banner: String,                  // صورة غلاف المتجر
  
  categories: [ObjectId],          // الأقسام التي يعمل بها
  
  contact: {
    phone: String,
    email: String,
    address: String,
    city: String,
    region: String
  },
  
  businessInfo: {
    commercialRegister: String,
    taxNumber: String,
    licenseUrl: String,            // رابط صورة الرخصة
    verificationStatus: String     // 'pending', 'approved', 'rejected'
  },
  
  commission: {
    rate: Number,                  // نسبة العمولة (0.10 = 10%)
    type: String                   // 'percentage', 'fixed'
  },
  
  rating: {
    average: Number,               // متوسط التقييم (0-5)
    count: Number                  // عدد التقييمات
  },
  
  stats: {
    totalProducts: Number,
    totalSales: Number,
    totalRevenue: Number
  },
  
  bankInfo: {                      // لسحب الأرباح
    bankName: String,
    iban: String,
    accountName: String
  },
  
  isActive: Boolean,
  isFeatured: Boolean,             // متجر مميز
  createdAt: Date,
  updatedAt: Date
}
```

#### 3. Products (المنتجات)

```javascript
{
  _id: ObjectId,
  storeId: ObjectId,               // معرف المتجر
  categoryId: ObjectId,            // معرف القسم
  
  name: {
    ar: String,
    en: String
  },
  slug: String,                    // للرابط
  description: {
    ar: String,
    en: String
  },
  
  images: [String],                // مصفوفة روابط الصور
  
  price: {
    amount: Number,                // السعر الأساسي
    currency: String,              // 'SAR'
    discount: {
      percentage: Number,          // نسبة الخصم
      validUntil: Date            // تاريخ انتهاء الخصم
    }
  },
  
  inventory: {
    quantity: Number,              // الكمية المتوفرة
    sku: String,                   // رمز المنتج
    trackInventory: Boolean
  },
  
  specifications: [{              // المواصفات
    name: { ar: String, en: String },
    value: { ar: String, en: String }
  }],
  
  dimensions: {                    // الأبعاد (للشحن)
    length: Number,
    width: Number,
    height: Number,
    weight: Number,
    unit: String                   // 'cm', 'kg'
  },
  
  seo: {
    metaTitle: String,
    metaDescription: String,
    keywords: [String]
  },
  
  rating: {
    average: Number,
    count: Number
  },
  
  stats: {
    views: Number,
    favorites: Number,
    sales: Number
  },
  
  isActive: Boolean,
  isFeatured: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

#### 4. Orders (الطلبات)

```javascript
{
  _id: ObjectId,
  orderNumber: String,             // رقم الطلب الفريد
  buyerUserId: ObjectId,
  
  items: [{
    productId: ObjectId,
    storeId: ObjectId,
    name: String,
    quantity: Number,
    price: Number,                 // السعر وقت الشراء
    commission: Number,            // العمولة المحسوبة
    image: String
  }],
  
  totals: {
    subtotal: Number,              // المجموع الفرعي
    shipping: Number,              // الشحن
    tax: Number,                   // الضريبة
    discount: Number,              // الخصم
    total: Number                  // الإجمالي
  },
  
  shippingAddress: {
    name: String,
    phone: String,
    street: String,
    city: String,
    region: String,
    postalCode: String
  },
  
  payment: {
    method: String,                // 'cod', 'visa', 'mada', 'apple_pay'
    status: String,                // 'pending', 'paid', 'failed', 'refunded'
    transactionId: String,
    paidAt: Date
  },
  
  shipping: {
    method: String,                // 'standard', 'express'
    trackingNumber: String,
    carrier: String,               // 'SMSA', 'Aramex'
    estimatedDelivery: Date,
    shippedAt: Date,
    deliveredAt: Date
  },
  
  status: String,                  // 'pending', 'processing', 'shipped', 'delivered', 'cancelled'
  
  notes: String,                   // ملاحظات العميل
  
  timeline: [{                     // سجل تحديثات الطلب
    status: String,
    note: String,
    timestamp: Date
  }],
  
  createdAt: Date,
  updatedAt: Date
}
```

---

## 🔐 نظام المصادقة الموحد (SSO) {#نظام-المصادقة}

### المفهوم

**Single Sign-On (SSO)** يعني أن المستخدم يسجل دخول **مرة واحدة** في المنصة الأم (`axiomhub.com`) ويمكنه الوصول لجميع الأنظمة الفرعية بدون تسجيل دخول متكرر.

### آلية العمل

```
1. المستخدم يزور: marketplace.axiomhub.com
   └─> يُحول تلقائياً لـ: axiomhub.com/login

2. يسجل دخول في المنصة الأم
   └─> الخادم يصدر JWT Token

3. Token يحتوي على:
   - userId
   - companyId
   - permissions (صلاحيات لكل نظام)

4. المستخدم يُحول لـ: marketplace.axiomhub.com?token=xyz
   └─> النظام الفرعي يتحقق من Token مع المنصة الأم

5. Token صحيح؟
   ✅ نعم → يسمح بالدخول
   ❌ لا → يعيد التوجيه للمنصة الأم
```

### تطبيق JWT Token

#### في المنصة الأم (axiomhub-core)

```javascript
// core-api/routes/auth.js

// تسجيل الدخول
POST /api/auth/login
Body: { email, password }

Response:
{
  success: true,
  token: "eyJhbGciOiJIUzI1NiIs...",
  user: {
    id: "...",
    name: "...",
    email: "...",
    companyId: "...",
    permissions: {
      inspect: ["read", "write"],
      marketplace: ["buyer"],
      orders: ["buyer", "approver"]
    }
  }
}
```

#### في النظام الفرعي (axiom-marketplace)

```javascript
// middleware/verifyToken.js

const verifyToken = async (req, res, next) => {
    const token = req.headers.authorization?.split(' ')[1];
    
    if (!token) {
        return res.redirect('https://axiomhub.com/login');
    }
    
    try {
        // التحقق من Token مع المنصة الأم
        const response = await fetch('http://localhost:3000/api/auth/verify', {
            headers: { 'Authorization': `Bearer ${token}` }
        });
        
        const data = await response.json();
        
        if (!data.valid) {
            return res.redirect('https://axiomhub.com/login');
        }
        
        // التحقق من الصلاحيات
        if (!data.user.permissions.marketplace) {
            return res.status(403).json({ error: 'No marketplace access' });
        }
        
        req.user = data.user;
        next();
    } catch (error) {
        return res.redirect('https://axiomhub.com/login');
    }
};
```

---

## 📅 خطة التنفيذ {#خطة-التنفيذ}

### المرحلة 0: إعادة الهيكلة (أسبوع واحد)

**الهدف:** تحويل المشروع الحالي إلى نظام بيئي متكامل

**المهام:**
1. ✅ إنشاء `axiomhub-core` (المنصة الأم)
2. ✅ نقل المشروع الحالي إلى `axiom-inspect`
3. ✅ إنشاء نظام المصادقة الموحد (SSO)
4. ✅ بناء الموارد المشتركة (Shared Resources)
5. ✅ ربط `axiom-inspect` بالمنصة الأم
6. ✅ اختبار التكامل

**المخرجات:**
- هيكل مجلدات منظم
- نظام SSO يعمل
- Inspect يعمل كنظام فرعي

---

### المرحلة 1: بناء Marketplace (MVP) (3 أسابيع)

#### الأسبوع 1: قاعدة البيانات والـ Backend

**المهام:**
1. ✅ إنشاء Models:
   - Store.js
   - Category.js
   - Product.js
   - Order.js
   - Cart.js
2. ✅ إنشاء Routes:
   - /api/stores
   - /api/products
   - /api/orders
   - /api/categories
3. ✅ إنشاء Controllers
4. ✅ اختبار API بـ Postman

#### الأسبوع 2: واجهة المشتري

**المهام:**
1. ✅ تصميم الصفحة الرئيسية (عرض الأقسام)
2. ✅ تصميم صفحة القسم (كروت المنتجات)
3. ✅ تصميم صفحة تفاصيل المنتج
4. ✅ تصميم سلة التسوق
5. ✅ تصميم صفحة الدفع

#### الأسبوع 3: واجهة البائع

**المهام:**
1. ✅ لوحة تحكم البائع (Dashboard)
2. ✅ إدارة المنتجات (إضافة/تعديل/حذف)
3. ✅ إدارة الطلبات
4. ✅ صفحة المالية (الأرباح)

---

### المرحلة 2: التكامل والاختبار (أسبوع واحد)

**المهام:**
1. ✅ ربط Marketplace بنظام SSO
2. ✅ اختبار سير الشراء الكامل
3. ✅ اختبار لوحة تحكم البائع
4. ✅ إصلاح الأخطاء
5. ✅ تحسين الأداء

---

### المرحلة 3: الإطلاق التجريبي (Beta) (أسبوعان)

**المهام:**
1. ✅ دعوة 10 بائعين للتسجيل
2. ✅ إضافة 100-200 منتج
3. ✅ دعوة 100 مستخدم للتجربة
4. ✅ جمع الملاحظات
5. ✅ إجراء التحسينات

---

## 🛒 السوق الكبير - تفاصيل إضافية {#السوق-الكبير}

### تصميم كارت المنتج (Product Card)

```html
<div class="product-card">
    <div class="product-image">
        <img src="product.jpg" alt="Product Name">
        <span class="badge-discount">-20%</span>
    </div>
    <div class="product-info">
        <h3 class="product-title">بلاط سيراميك إسباني فاخر</h3>
        <div class="product-store">
            <i class="fas fa-store"></i>
            <span>متجر الأخشاب الذهبي</span>
        </div>
        <div class="product-rating">
            <i class="fas fa-star"></i>
            <span>4.5 (120 تقييم)</span>
        </div>
        <div class="product-price">
            <span class="old-price">200 ريال</span>
            <span class="current-price">160 ريال</span>
        </div>
        <button class="btn-add-cart">
            <i class="fas fa-cart-plus"></i> أضف للسلة
        </button>
    </div>
</div>
```

### نماذج الأعمال المدعومة

#### 1. نموذج العمولة (Commission Model)

**كيفية العمل:**
- البائع لا يدفع رسوم تسجيل
- يدفع نسبة من كل عملية بيع ناجحة
- النسبة: 5-10% حسب القسم
- الدفع للبائع: كل أسبوعين

**المزايا:**
- ✅ لا حواجز أمام البائعين الجدد
- ✅ المنصة تربح فقط عند نجاح البائع
- ✅ يشجع البائعين على زيادة المبيعات

**العيوب:**
- ⚠️ صعوبة التنبؤ بالدخل
- ⚠️ يحتاج نظام محاسبة دقيق

#### 2. نموذج الاشتراك (Subscription Model)

**كيفية العمل:**
- البائع يدفع رسوم شهرية ثابتة
- عمولة أقل (2-5%) أو بدون عمولة
- ثلاث خطط: Basic (99 ريال), Pro (299 ريال), Enterprise (999 ريال)

**المزايا:**
- ✅ دخل ثابت ومتوقع
- ✅ يجذب البائعين الجادين
- ✅ أسهل في الإدارة

**العيوب:**
- ⚠️ حاجز أمام البائعين الصغار
- ⚠️ يحتاج جذب بائعين بسرعة

**القرار المقترح:** نبدأ بنموذج العمولة في MVP، ثم نضيف الاشتراك لاحقاً.

---

## 🚀 الخطوات التالية الموصى بها

### الخيار الأول: البدء بإعادة الهيكلة (مقترح) ⭐

1. إنشاء `axiomhub-core`
2. نقل المشروع الحالي لـ `axiom-inspect`
3. بناء نظام SSO
4. اختبار التكامل
5. **ثم** البدء ببناء Marketplace

**المدة الزمنية:** أسبوع للهيكلة + 3 أسابيع للـ Marketplace = **شهر واحد**

### الخيار الثاني: البدء بـ Marketplace مباشرة

1. إنشاء `axiom-marketplace` بجانب المشروع الحالي
2. استخدام نفس نظام المصادقة الموجود
3. البناء السريع
4. إعادة الهيكلة لاحقاً

**المدة الزمنية:** 3 أسابيع للـ Marketplace

---

## 📊 مقارنة الخيارات

| المعيار | إعادة الهيكلة أولاً | Marketplace مباشرة |
|---------|---------------------|-------------------|
| **الوقت** | أطول (شهر) | أسرع (3 أسابيع) |
| **الجودة** | ⭐⭐⭐⭐⭐ بنية احترافية | ⭐⭐⭐ بنية مؤقتة |
| **قابلية التوسع** | ⭐⭐⭐⭐⭐ ممتازة | ⭐⭐⭐ محدودة |
| **الصيانة** | ⭐⭐⭐⭐⭐ سهلة جداً | ⭐⭐⭐ صعبة لاحقاً |
| **المخاطر** | ⭐⭐ منخفضة | ⭐⭐⭐⭐ عالية (دين تقني) |

**التوصية النهائية:** **إعادة الهيكلة أولاً** ⭐

---

## 📝 الملاحظات الختامية

هذه الدراسة تمثل خارطة طريق شاملة لبناء نظام بيئي رقمي متكامل. المشروع طموح لكنه قابل للتنفيذ بالتخطيط السليم والتنفيذ المرحلي.

**النصائح الرئيسية:**
1. ✅ ابدأ صغيراً (MVP) ثم وسّع
2. ✅ اختبر كل مرحلة جيداً قبل الانتقال للتالية
3. ✅ استمع لملاحظات المستخدمين الأوائل
4. ✅ لا تحاول بناء كل شيء دفعة واحدة
5. ✅ وثّق كل قرار تقني مهم

**تاريخ آخر تحديث:** 6 نوفمبر 2025  
**الإصدار:** 1.0  
**الحالة:** جاهز للتنفيذ 🚀
