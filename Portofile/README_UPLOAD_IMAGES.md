# 📤 دليل رفع صور المشاريع

## الخطوات

### 1. تجهيز الصور
```
Portofile/
  └── assets/
      └── images/
          └── projects/    ← ضع صور المشاريع هنا
              ├── project1.jpg
              ├── project2.png
              └── project3.webp
```

### 2. تشغيل الـ API
```powershell
cd api
npm start
```
يجب أن يعمل على `http://localhost:3001`

### 3. تشغيل سكريبت الرفع
```powershell
.\upload-images.ps1
```

## ماذا يفعل السكريبت؟

1. ✅ يبحث عن كل الصور في `assets/images/projects/`
2. 🔐 يسجل الدخول إلى API
3. 📤 يرفع كل صورة ويحصل على 3 نسخ محسّنة:
   - **thumbnail** (400px) - للمعاينة السريعة
   - **medium** (800px) - للعرض العادي
   - **large** (1200px) - للمعرض الكبير
4. 💾 يحفظ قائمة الروابط في ملف JSON

## النتيجة

سيتم إنشاء ملف مثل `uploaded-images-2025-11-27-143055.json`:

```json
[
  {
    "original": "project1.jpg",
    "url": "/uploads/1732716655000-a1b2c3-medium.webp",
    "thumbnail": "/uploads/1732716655000-a1b2c3-thumb.webp",
    "medium": "/uploads/1732716655000-a1b2c3-medium.webp",
    "large": "/uploads/1732716655000-a1b2c3-large.webp"
  }
]
```

## تخصيص الإعدادات

افتح `upload-images.ps1` وعدل:

```powershell
$API_URL = "http://localhost:3001/api"  # رابط API
$IMAGES_DIR = ".\assets\images\projects"  # مجلد الصور
$USERNAME = "admin"                     # اسم المستخدم
$PASSWORD = "admin123"                  # كلمة المرور
```

## للرفع إلى Hostinger

عندما يكون الموقع على Hostinger، عدل فقط:

```powershell
$API_URL = "https://yourdomain.com/api"
```

## استخدام الروابط

بعد الرفع، استخدم الروابط في `content.json`:

```json
{
  "projects": [
    {
      "id": "project-1",
      "thumbnail": "/uploads/1732716655000-a1b2c3-medium.webp",
      "gallery": [
        {
          "url": "/uploads/1732716655000-a1b2c3-large.webp"
        }
      ]
    }
  ]
}
```

## ملاحظات

- 📦 الصور المدعومة: `.jpg`, `.jpeg`, `.png`, `.webp`
- 📏 الحد الأقصى: 10 ميجابايت للصورة
- 🗜️ يتم ضغط الصور تلقائياً إلى WebP
- 💾 يتم الحفظ في `api/uploads/`
